name: CI

on: 
  push:
  pull_request:
      branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      
      - name: Install uv
        uses: astral-sh/setup-uv@v8
        with:
          python-version: "3.12"
          enable-cache: true
      
      - name: Intall the project and dev tools
        run: uv sync --extra dev

      - name: Lint with ruff
        run: uv run ruff check
      
      - name: Check formating with ruff
        run: uv run ruff format --check

      - name: Run tests with pytest
        run: uv run pytest
      